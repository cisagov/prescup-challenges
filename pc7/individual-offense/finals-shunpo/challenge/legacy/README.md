# Legacy Content

This directory preserves original or superseded SHUNPO materials that are no longer part of the active deployment path:

- `coap-sim-original/` — original inert CoAP simulator
- `evidence-original/` — original static evidence files and replay capture artifacts
- `replay-pcap-original/` — original mixed replay pcap
- `dashboard-replay-original/` — original local replay runner
- `dashboard-static-index-original.html` — original dashboard landing-page asset

The live challenge now uses the container directories at the root of `challenge/`:

- `dashboard/`
- `replay/`
- `ops-panel/`
- `coap-bridge/`
